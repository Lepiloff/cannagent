# Ежедневный флоу работы с AI Budtender

## 🚀 Быстрый старт (после перезагрузки)

### Вариант 1: Локальная БД (РЕКОМЕНДУЕТСЯ)

```bash
# 1. Запустить cannamente (если не запущен)
cd ../cannamente && docker-compose up -d

# 2. Запустить AI Budtender с локальной БД
cd ai_budtender && make setup-local
```

**Готово!** Теперь у вас:
- ✅ Локальная БД с pgvector (данные сохраняются)
- ✅ Синхронизированные данные из cannamente
- ✅ Работающий API на http://localhost:8001

### Вариант 2: Внешняя БД (текущий)

```bash
# 1. Запустить cannamente
cd ../cannamente && docker-compose up -d

# 2. Запустить AI Budtender
cd ai_budtender && make start

# 3. Установить pgvector (после перезагрузки)
make setup-pgvector
```

## 📊 Проверка работы

```bash
# Проверить статус
make status

# Проверить подключение к БД
make check-db

# Тест API
curl http://localhost:8001/api/v1/ping/
```

## 🔄 Синхронизация новых данных

### Автоматическая синхронизация (рекомендуется)

```bash
# Запустить в фоне (мониторинг каждые 30 секунд)
make watch-cannamente

# Или в отдельном терминале:
nohup make watch-cannamente > sync.log 2>&1 &
```

### Ручная синхронизация

```bash
# Синхронизировать все данные
make sync-cannamente

# Синхронизировать только новые strains
make sync-new
```

## 🛑 Остановка

```bash
# Остановить AI Budtender
make stop-local  # для локальной БД
make stop        # для внешней БД

# Остановить cannamente
cd ../cannamente && docker-compose down
```

## 📝 Ежедневные команды

### Утром (после включения компьютера)

```bash
# 1. Запустить cannamente
cd ../cannamente && docker-compose up -d

# 2. Запустить AI Budtender
cd ai_budtender && make setup-local

# 3. Проверить работу
make status
curl http://localhost:8001/api/v1/ping/
```

### В течение дня

```bash
# Проверить новые strains
make sync-new

# Посмотреть логи
make logs

# Проверить статус
make status
```

### Вечером

```bash
# Остановить сервисы
make stop-local
cd ../cannamente && docker-compose down
```

## 🔧 Устранение проблем

### Проблема: БД не подключается

```bash
# Проверить cannamente
docker ps | grep canna

# Проверить подключение
make check-db

# Перезапустить
make restart
```

### Проблема: Нет новых данных

```bash
# Принудительная синхронизация
make sync-cannamente

# Проверить новые strains
make sync-new
```

### Проблема: pgvector не работает

```bash
# Для внешней БД
make setup-pgvector

# Для локальной БД - пересоздать
make stop-local
make start-local
```

## 📈 Мониторинг

```bash
# Статус сервисов
make status

# Логи в реальном времени
make logs

# Метрики
curl http://localhost:8001/metrics

# Статистика кеша
curl http://localhost:8001/api/v1/cache/stats/
```

## 🎯 Тестирование API

```bash
# Тест чата
curl -X POST http://localhost:8001/api/v1/chat/ask/ \
  -H "Content-Type: application/json" \
  -d '{"message": "Recommend something for relaxation", "history": []}'

# Получить продукты
curl http://localhost:8001/api/v1/products/

# Очистить кеш
curl -X POST http://localhost:8001/api/v1/cache/clear/
```

## 💡 Полезные команды

```bash
# Открыть shell в контейнере
make shell

# Открыть Redis CLI
make redis-cli

# Запустить тесты
make test

# Форматировать код
make format
```

## 🔄 Архитектура данных

```
┌─────────────────┐    ┌──────────────────┐    ┌─────────────────┐
│   Cannamente    │    │   AI Budtender   │    │     Client      │
│   (Client DB)   │───▶│   (Local DB)     │───▶│   (Frontend)    │
│                 │    │                  │    │                 │
│ - strains data  │    │ - Vector search  │    │ - Chat UI       │
│ - READ ONLY     │    │ - AI processing  │    │ - API calls     │
│ - GET/SELECT    │    │ - Cached data    │    │                 │
└─────────────────┘    └──────────────────┘    └─────────────────┘
```

**Преимущества локальной БД:**
- ✅ Данные сохраняются после перезагрузки
- ✅ pgvector всегда доступен
- ✅ Быстрый векторный поиск
- ✅ Независимость от внешних сервисов
- ✅ Автоматическая синхронизация новых данных 