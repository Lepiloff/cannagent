# Canagent Integration Guide для Cannamente

## Smart Query Executor v3.0 - Медицински-осознанный AI Budtender

### Ключевые улучшения: Приоритет медицинских показаний + Универсальный подход

**Было (v2.0):** Хардкод с медицинскими ошибками
```javascript
// ❌ Старый подход - мог рекомендовать Acapulco Gold (энергетик) для бессонницы
if (query.includes("insomnia")) {
  return getIndicas(); // Примитивная логика
}
```

**Стало (v3.0):** Медицински-осознанный AI с приоритетной системой
```javascript
// ✅ AI анализирует медицинские противопоказания:
{
  "filters": {
    "helps_with": {"operator": "contains", "values": ["Insomnia"], "priority": 1}, // МЕДИЦИНСКИЙ ПРИОРИТЕТ
    "effects": {"operator": "not_contains", "values": ["Energetic", "Uplifted"], "priority": 1}, // ИСКЛЮЧАЕТ ПРОТИВОРЕЧИЯ
    "thc": {"operator": "gte", "value": 15, "priority": 2} // Вторичный критерий
  },
  "scoring": {"method": "weighted_priority"} // Медицинский скоринг
}
```

## Медицинские улучшения безопасности

### 1. Система медицинских приоритетов (Priority 1)
- **Медицинские показания:** `helps_with` (Insomnia, Anxiety, Pain) = Priority 1 (10x вес)
- **Противопоказания:** Автоматическое исключение противоречивых эффектов
- **Пример:** Для бессонницы исключаются "Energetic", "Uplifted", "Creative"

### 2. Решение проблемы с недостоверными данными
- **Было:** Italian Ice (THC: N/A) в "highest THC" запросах
- **Стало:** Автоматическая фильтрация null/N/A/invalid значений
- **Бонус:** Качественные данные получают приоритетный скоринг

### 3. Универсальная медицински-осознанная обработка
- "high THC indica for insomnia" → Приоритет: медицинские показания > THC уровень
- "anxiety relief with CBD" → Исключает "Paranoid", "Anxious" побочные эффекты
- "depression help" → ВКЛЮЧАЕТ "Uplifted", "Happy" (полезно для настроения)
- "energy boost" → Исключает "Sleepy", "Couch Lock"

### 4. Context-Aware Architecture с медицинским контекстом
```javascript
// API теперь автоматически:
- Сохраняет медицинские предпочтения между сообщениями
- Восстанавливает полный контекст сессии (Redis-backed)
- Отслеживает медицинские противопоказания в истории
- Генерирует релевантные follow-up предложения
- Объясняет медицинские решения (при debug=true)
```

## Новое API v3.0 - Медицински-осознанные ответы

### Расширенные поля ответа с медицинскими метаданными

```json
{
  "response": "Рекомендую Northern Lights для релаксации и сна. Этот индика-сорт...",
  "recommended_strains": [
    {
      "id": 42, "name": "Northern Lights", "thc": "18.50", "cbd": "0.10",
      "category": "Indica", "medical_score": 0.95, // НОВОЕ: медицинский скоринг
      "helps_with": [{"name": "Insomnia"}, {"name": "Stress"}],
      "contradictions_avoided": ["Energetic", "Uplifted"] // НОВОЕ: что исключено
    }
  ],
  "action_executed": "search_strains", // Что именно AI сделал
  "filters_applied": {
    "helps_with": {"operator": "contains", "values": ["Insomnia"], "priority": 1},
    "effects": {"operator": "not_contains", "values": ["Energetic"], "priority": 1}
  },
  "scoring_method": "weighted_priority", // НОВОЕ: тип медицинского скоринга 
  "medical_warnings": [], // НОВОЕ: предупреждения о противопоказаниях
  "session_id": "uuid",
  "confidence": 0.92,
  "reasoning": "Prioritized medical indications over THC level" // НОВОЕ: объяснение AI
}
```

### Конфигурация среды

```env
# Smart Query Executor v3.0 (уже настроено)
USE_SMART_EXECUTOR=true
SMART_EXECUTOR_TIMEOUT=5000
ENABLE_AI_REASONING_DEBUG=false  # true для отладки
```

## Тестирование интеграции

### Тест 1: Базовый поиск
```bash
curl -X POST "http://localhost:8001/api/v1/chat/ask" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "suggest me strains for relaxing and good sleep"
  }'
```

### Тест 2: Медицинские приоритеты (ключевое улучшение)
```bash
curl -X POST "http://localhost:8001/api/v1/chat/ask" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "high THC strains for insomnia",
    "session_id": "SESSION_ID_FROM_STEP_1"
  }'
# Ожидаемый результат: AI приоритизирует медицинские показания над THC
# Исключит энергетичные сативы (Acapulco Gold), предложит индику/гибриды
```

### Тест 3: Медицинские противоречия
```bash
curl -X POST "http://localhost:8001/api/v1/chat/ask" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "anxiety relief with high CBD but no paranoia effects"
  }'
# AI автоматически исключит сорта с "Paranoid", "Anxious" побочными эффектами
```

### Тест 4: Follow-up с сохранением медицинского контекста
```bash
curl -X POST "http://localhost:8001/api/v1/chat/ask" \
  -H "Content-Type: application/json" \
  -d '{
    "message": "which one has lowest THC",
    "session_id": "SAME_SESSION_ID"
  }'
# AI отфильтрует из предыдущих рекомендаций по THC, сохранив медицинские критерии
```

## Критические улучшения v3.0

### 🏥 Медицинская безопасность (приоритет #1):
1. **Противопоказания исключены** - нет больше энергетиков для бессонницы
2. **Медицинские приоритеты** - показания важнее THC/вкуса 
3. **Качество данных** - автоматическое исключение THC: N/A
4. **Scoring система** - медицинские совпадения получают 10x вес
5. **Penalty система** - медицинские нарушения штрафуются, но не исключаются полностью

### 🧠 AI-driven универсальность:
1. **Follow-up запросы** - контекст сохраняется между сообщениями
2. **Сложные запросы** - любые комбинации полей без хардкода
3. **Natural language** - пользователи говорят естественно
4. **Session восстановление** - Redis-backed персистентность
5. **Smart prioritization** - Priority 1 (медицина) > Priority 2 (THC/CBD) > Priority 3 (вкусы)

### 🎯 Медицинские возможности:
- **Медицинские консультации**: "help with chronic pain and inflammation"
- **Избежание противопоказаний**: "anxiety without paranoia side effects"  
- **Комбинированная терапия**: "depression and insomnia relief"
- **Дозировка осознание**: "high CBD low THC for daily use"

### 🚀 Технические преимущества:
- **Нулевое программирование** для новых медицинских запросов
- **AI генерирует критерии** для любых комбинаций
- **Универсальные операторы** (eq, gte, contains, not_contains)
- **Weighted scoring** с медицинскими приоритетами

### 📊 Компоненты архитектуры v3.0:
- **SmartQueryAnalyzer**: AI-анализ с медицинскими guidelines
- **UniversalActionExecutor**: Медицински-осознанная фильтрация и скоринг
- **ContextProvider**: Полный контекст включая медицинские предпочтения
- **SmartRAGService**: Интеграция всех компонентов

---

**Итог:** Smart Query Executor v3.0 превращает AI Budtender в медицински-осознанную систему рекомендаций, которая приоритизирует безопасность пациентов над простым поиском по критериям, решая при этом исходные проблемы с контекстом и универсальностью.